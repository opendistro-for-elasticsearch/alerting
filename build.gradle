apply plugin: 'base'

ext.es_version = '6.4.0'

evaluationDependsOnChildren()
task 'build-all-plugins'(type: Sync) {
    from project(':monitoring').tasks.bundlePlugin
    into 'build/distributions'
}
assemble.dependsOn('build-all-plugins')